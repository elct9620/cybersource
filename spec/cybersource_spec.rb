require "spec_helper"

describe Cybersource do
  it "has a version number" do
    expect(Cybersource::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
